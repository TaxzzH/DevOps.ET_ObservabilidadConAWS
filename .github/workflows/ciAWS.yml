# name: CI/CD Microservicio Suscripciones

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   build-push-deploy-suscripciones:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout código
#       uses: actions/checkout@v3

#     - name: Configurar credenciales AWS
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
#         aws-region: ${{ secrets.AWS_REGION }}

#     - name: Login en Amazon ECR (runner)
#       uses: aws-actions/amazon-ecr-login@v1

#     - name: Build imagen Suscripciones y push a ECR
#       run: |
#         cd suscripciones 
#         docker build -t suscripciones .
#         docker tag suscripciones:latest \
#           ${{ secrets.ECR_REPO_URL_SUSCRIPCIONES }}:latest
#         docker push \
#           ${{ secrets.ECR_REPO_URL_SUSCRIPCIONES }}:latest

#     - name: Actualizar Tag de la imagen en la Task Def
#       id: render-task-def
#       uses: suscripciones/task-definition.json
#       with:
#         task-definition: task-definition.json
#         container-name: micro-suscripciones 
#         image: ${{ secrets.ECR_REPO_URL_SUSCRIPCIONES }}:latest

#     - name: Desplegar nueva Task Definition en el Servicio
#       uses: aws-actions/amazon-ecs-deploy-task-definition@v2
#       with:
#         task-definition: ${{ steps.render-task-def.outputs.task-definition }}
#         service: ${{ secrets.ECS_SERVICE_NAME }}
#         cluster: ${{ secrets.ECS_CLUSTER_NAME }}
#         wait-for-service-stability: true

# Ni idea si algo de esto funciona btw.